import { Mapbox.StyleDirective } from './mapbox.style.directive';

describe('Mapbox.StyleDirective', () => {
  it('should create an instance', () => {
    const directive = new Mapbox.StyleDirective();
    expect(directive).toBeTruthy();
  });
});
