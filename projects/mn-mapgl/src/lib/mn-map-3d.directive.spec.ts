import { MnMap3dDirective } from './mn-map-3d.directive';

describe('MnMap3dDirective', () => {
  it('should create an instance', () => {
    const directive = new MnMap3dDirective();
    expect(directive).toBeTruthy();
  });
});
