import { MnLazyDirective } from './mn-lazy.directive';

describe('MnLazyDirective', () => {
  it('should create an instance', () => {
    const directive = new MnLazyDirective();
    expect(directive).toBeTruthy();
  });
});
