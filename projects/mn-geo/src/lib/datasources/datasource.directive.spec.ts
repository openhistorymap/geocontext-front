import { DatasourceDirective } from './datasource.directive';

describe('DatasourceDirective', () => {
  it('should create an instance', () => {
    const directive = new DatasourceDirective();
    expect(directive).toBeTruthy();
  });
});
