/**
 * Engine version surfaced in the toolbar/footer. Kept in lockstep with
 * `projects/gcx-core/package.json#version` and bumped together at release
 * time. Imported by `<gcx-main>` so the published library carries its own
 * version into the UI.
 */
export const GCX_VERSION = '0.1.0';
