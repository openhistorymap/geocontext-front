import { MnMap2dDirective } from './mn-map-2d.directive';

describe('MnMap2dDirective', () => {
  it('should create an instance', () => {
    const directive = new MnMap2dDirective();
    expect(directive).toBeTruthy();
  });
});
